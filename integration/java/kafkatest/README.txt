This Maven project creates an executable Java fatjar that runs a Java Kafka client for the producer_endpoint
test.
To rebuild the fatjar use:
`mvn clean package -DskipTests`
Apache Maven and a JDK must be installed first.
`brew install maven` on MacOS to install Maven